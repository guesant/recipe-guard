{
  "private": true,
  "name": "recipe-guard",
  "workspaces": [
    "packages/core",
    "packages/typeorm"
  ],
  "scripts": {
    "build": "lerna run build",
    "prepare": "npm run build",
    "dev": "lerna run --parallel dev"
  },
  "devDependencies": {
    "lerna": "^7.3.0"
  }
}
